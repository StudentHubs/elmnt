import * as React from 'react';
import r from 'refluent';
export interface PlaceholderProps {
    text?: string;
    value: string;
    prompt?: boolean;
    style: React.CSSProperties;
}
declare const _default: r<PlaceholderProps, PlaceholderProps>;
export default _default;
